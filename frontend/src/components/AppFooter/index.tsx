
import { Layout } from 'antd';
import './index.scss';

const { Footer } = Layout;

const AppFooter = () => {
  
  return (
    <Footer className="footer" style={{ textAlign: 'center' }} />
  );
};
export default AppFooter;