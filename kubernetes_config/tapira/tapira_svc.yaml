apiVersion: v1
kind: Service
metadata:
  name: tapira
  labels:
    name: tapira
  namespace: apiclarity
spec:
  type: NodePort
  ports:
    - port: 80
      protocol: TCP
      targetPort: 5000
      nodePort: 30002
  selector:
    name: tapira